import { GameCase } from "@/types"
import { blackwoodManor } from "./blackwood-manor"
import { harborLight } from "./harbor-light"
import { redThread } from "./red-thread"
import { viennaProtocol } from "./vienna-protocol"
import { eclipseProtocol } from "./eclipse-protocol"
import { ashwoodCovenant } from "./ashwood-covenant"

export const ALL_CASES: GameCase[] = [
  blackwoodManor,
  harborLight,
  redThread,
  viennaProtocol,
  eclipseProtocol,
  ashwoodCovenant,
]

export { blackwoodManor, harborLight, redThread, viennaProtocol, eclipseProtocol, ashwoodCovenant }
